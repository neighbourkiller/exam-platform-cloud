spring:
  datasource: { url: "jdbc:mysql://${MYSQL_HOST:mysql}:3306/exam_grading?serverTimezone=Asia/Shanghai", username: "${DB_USERNAME:exam}", password: "${DB_PASSWORD:exam}" }
  flyway: { enabled: true, locations: "classpath:db/migration" }
app:
  rabbitmq:
    grading-submission:
      retry:
        max-attempts: ${GRADING_SUBMISSION_RETRY_MAX_ATTEMPTS:3}
        initial-interval-ms: ${GRADING_SUBMISSION_RETRY_INITIAL_INTERVAL_MS:1000}
        multiplier: ${GRADING_SUBMISSION_RETRY_MULTIPLIER:2.0}
        max-interval-ms: ${GRADING_SUBMISSION_RETRY_MAX_INTERVAL_MS:10000}
